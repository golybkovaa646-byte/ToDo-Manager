<UserControl x:Class="ToDo_Manager.View.Controls.TagsPageElements.CreateTagPanelTagsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Height="Auto" Width="Auto">

    <UserControl.Resources>

        <!-- Градиентный фон карточки -->
        <LinearGradientBrush x:Key="PanelBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="1"/>
            <GradientStop Color="#F9F6F3" Offset="0.3"/>
        </LinearGradientBrush>

        <!-- Тень -->
        <DropShadowEffect x:Key="PanelShadow"
                          Color="#000000"
                          BlurRadius="12"
                          ShadowDepth="3"
                          Opacity="0.15"/>

        <!-- Стиль TextBox -->
        <Style TargetType="TextBox" x:Key="InputBoxStyle">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль кнопки -->
        <Style TargetType="Button" x:Key="AddButtonStyle">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#43A047"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Border Background="{StaticResource PanelBackground}"
            CornerRadius="14"
            Padding="30"
            Margin="0,0,15,0"
            BorderBrush="#E5E5E5"
            BorderThickness="1"
            Effect="{StaticResource PanelShadow}">

        <StackPanel>

            <!-- Заголовок -->
            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="Create Tag"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>

                <TextBlock Text="Add a new tag to your list tasks"
                           FontSize="14"
                           Foreground="#777"/>
            </StackPanel>

            <!-- Tag Name -->
            <TextBlock Text="Tag Name" FontWeight="Bold" FontSize="16"/>
            <TextBox Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource InputBoxStyle}"
                     Margin="0,0,0,20"/>

            <!-- Color HEX -->
            <TextBlock Text="Color HEX" FontWeight="Bold" FontSize="16"/>
            <xctk:ColorPicker SelectedColor="{Binding NewTagColor, Mode=TwoWay}"
                  Width="200"
                  Height="40"
                  Margin="0,0,0,25"
                  UsingAlphaChannel="False"
                  BorderBrush="#D0D0D0"
                  BorderThickness="1"/>


            <!-- Button -->
            <Button Content="Add Tag"
                    Command="{Binding AddTagCommand}"
                    Style="{StaticResource AddButtonStyle}"
                    Height="48"/>
        </StackPanel>
    </Border>

</UserControl>
