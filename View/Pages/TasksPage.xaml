<Page x:Class="ToDo_Manager.View.Pages.TasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ToDo_Manager.View.Pages"
      xmlns:vm="clr-namespace:ToDo_Manager.ViewModels"
      xmlns:models="clr-namespace:ToDo_Manager.Models"
      xmlns:controls="clr-namespace:ToDo_Manager.View.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TasksPage">

    <Page.Resources>
        <DropShadowEffect x:Key="DropShadow"
                       Color="Black"
                       BlurRadius="10"
                       ShadowDepth="2"
                       Opacity="0.2"/>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ЛЕВАЯ ПАНЕЛЬ -->
        <controls:CreateTaskPanelMainWindow Grid.Column="0"/>

        <!-- ПРАВАЯ ПАНЕЛЬ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ФИЛЬТРЫ -->
            <controls:FiltersPanelMainWindow Grid.Row="0"/>

            <!-- СПИСОК ЗАДАЧ -->
            <ListBox Grid.Row="1"
                  ItemsSource="{Binding FilteredTasks}"
                  Margin="0,15,0,0"
                  HorizontalContentAlignment="Stretch"
                  
                    
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:TaskItem}">
                        <controls:TaskCard />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Page>
